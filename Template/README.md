Each language can contain a language-specific README file, which is intended to contain essential information such as a translated installation guide to users.

This file will be placed as `<data>/Language/sigma-axis/<lang-name>.README.md` in the `.au2pkg.zip` archive and the user environments.
